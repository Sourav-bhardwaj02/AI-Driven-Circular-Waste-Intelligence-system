# Frontend Environment Variables — copy to .env (this file is safe to commit)
VITE_API_BASE_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
# No API keys needed here — all external routing APIs are proxied through the backend
