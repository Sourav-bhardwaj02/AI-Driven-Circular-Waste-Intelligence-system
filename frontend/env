# API Configuration
VITE_API_BASE_URL=http://localhost:5000/api
VITE_PORT=5173
VITE_API_URL2=http://localhost:8000/api
# Environment
VITE_NODE_ENV=development
VITE_SOCKET_URL=http://localhost:5000
# Routing Configuration
# External routing API calls are proxied through the backend for security.
# Frontend ONLY calls its own backend /api/routing/route — no keys needed here.
VITE_ROUTING_PROVIDER=osrm